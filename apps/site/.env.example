# Base URL of the API worker (exposed to the client via the PUBLIC_ prefix)
PUBLIC_API_URL=http://localhost:8787

# URL of the frontend app (apps/web)
PUBLIC_FRONTEND_URL=http://localhost:5173

# URL of the marketing site (apps/site)
PUBLIC_SITE_URL=http://localhost:4321
