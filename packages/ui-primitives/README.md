# @workspace/ui-primitives

Low-level, **unstyled React primitives** — the atomic building blocks beneath [`@workspace/ui`](../ui).

## Role

These are headless/minimal components with no Tailwind and no higher-level styling decisions. The styled design system (`@workspace/ui`) and app-wide logic (`@workspace/app`) build on top of them.

## What's here

- `components/` — primitive components, exported via `./components/*`

## Stack

React 19 only — intentionally dependency-light.
