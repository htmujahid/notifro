# @workspace/auth

> 🚧 **Planned** — not yet implemented.

Shared authentication primitives (Better Auth configuration, types, and helpers) to be reused across [`apps/api`](../../apps/api) and the client auth in [`@workspace/app`](../app). Today the auth clients live in `@workspace/app`; this package is intended to hold the cross-cutting, server-and-client-shared pieces.
